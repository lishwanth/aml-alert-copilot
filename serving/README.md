# serving/

LLM serving setup.

Planned:
- `vllm_setup.md` — steps to stand up vLLM on a temporary AWS GPU instance (dev/benchmark exercise, not the always-on demo backend)
- `groq_client.py` — Groq-hosted inference client used by the public demo (see docs/ARCHITECTURE.md for the trade-off rationale)
