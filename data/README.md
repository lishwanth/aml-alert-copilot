# data/

Not committed to git (see .gitignore) — this is where the fetched IBM AML dataset and DuckDB staging output live locally. Run `data_pipeline/fetch_kaggle_data.py` (once written) to populate this directory.
